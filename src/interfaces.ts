export interface ProductItem {
  id: string;
  isFav: boolean;
  title: string;
  description: string;
}

export interface Product {
  id: string;
  isFavorite: boolean;
  title: string;
  description: string;
}